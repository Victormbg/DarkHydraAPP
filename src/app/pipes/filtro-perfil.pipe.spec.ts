import { FiltroPerfilPipe } from './filtro-perfil.pipe';

describe('FiltroPerfilPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPerfilPipe();
    expect(pipe).toBeTruthy();
  });
});
